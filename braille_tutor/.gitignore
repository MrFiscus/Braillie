.venv*/
__pycache__/
.DS_Store

# cloned separately, see README (contains ~95 MB of weights and is its own git repo)
third_party/

# files the tools write while you run them
cells.json
calibration.json
calibration.png
check.png
frame.png
screenshot.png
*_annotated.png

# regenerable: python make_sheet.py / python make_markers.py (the printable markers/sheet.png and sheet/poke_*.png ARE kept)
sheet/face_*.png
sheet/nomarkers/face_*.png
markers/marker_*.png
